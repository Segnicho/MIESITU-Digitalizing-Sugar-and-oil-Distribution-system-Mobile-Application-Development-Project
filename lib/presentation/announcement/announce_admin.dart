import 'package:flutter/material.dart';

class AdminAnnounce extends StatelessWidget {
  const AdminAnnounce({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("admin announce page"),
      ),
      body: ListView(),
    );
  }
}
