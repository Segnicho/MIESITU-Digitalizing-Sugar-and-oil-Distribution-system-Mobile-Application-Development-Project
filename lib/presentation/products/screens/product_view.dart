import 'package:flutter/material.dart';

class AdminProduct extends StatelessWidget {
  const AdminProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("admin product view")),
    );
  }
}
